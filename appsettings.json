{
  "Logging": {
    "LogLevel": {
      "Default": "Trace",
      "Microsoft": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "EmployeeDBConnection": "Server=localhost;Initial Catalog=EmployeeDB;Trusted_Connection=true"
  }
}
//Trusted_Connection means integrated windows authentication or integarted security = SSPI or true
/*setting will be overrided in this order
  1.appsettings.json and appsettings.{env}.json
  2.user secrets
  env variables in launchsettings.json
  command-line arguments
  ------------------------
  we can also set in os level in prod or stage environments, by default value is prod
  for dev machine we set in lauchsettings.json


*/
